\title{$title$}

$if(title-running)$
\titlerunning{$title-running$}
$endif$

$if(blind-review)$
\author{Author information scrubbed for double-blind reviewing}
$else$
\author{$for(by-author)$$if(it.name.literal)$$it.name.literal$$endif$$if(it.affiliations)$\inst{$for(it.affiliations)$$it.number$$sep$,$endfor$}$endif$$if(it.attributes.corresponding)$\corr$endif$$if(it.orcid)$\orcidID{$it.orcid$}$endif$$sep$ \and $endfor$}

$if(author-running)$
\authorrunning{$author-running$}
$endif$

\institute{$for(by-affiliation)$$it.name$$if(it.address)$, $it.address$$if(it.postal-code)$, $it.postal-code$$endif$$if(it.city)$ $it.city$$endif$, $if(it.country)$$it.country$$endif$ $endif$$if(it.url)$ \email{$it.url$}$endif$$sep$ \and $endfor$}

$endif$